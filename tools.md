# Tools

- Use `brew list` to check for installed brew packages
- Use `python3 -c 'help("modules")'` to check for installed Python packages
- Use `npm list -g` to check for all installed node packages.